{
  "scripts": {
    "vercel-build": "cd my-project && npm install --legacy-peer-deps && npm run build",
    "build": "cd my-project && npm install --legacy-peer-deps && npm run build"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "axios": "^1.18.1",
    "react-hot-toast": "^2.6.0",
    "react-router-dom": "^7.18.1",
    "tailwindcss": "^4.3.2"
  }
}
